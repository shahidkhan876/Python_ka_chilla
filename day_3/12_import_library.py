import math
print("the value of pi:", math.pi)
import statistics
x = [100,200,50,50]
print("mean of the list:", statistics.mean(x))
print("mean of the list:", statistics.mean(x))
print("mean of the list:", statistics.mode(x))